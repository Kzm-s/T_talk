<div>
<%# ログインしているときは以下を表示する %>
    <% if user_signed_in? %>
    <div> 
        <%= "こんにちは、ようこそ" %>
    </div>
    <% end %>
</div>

<h3>コメント一覧</h3>
<% @posts.each do |c| %>
  <%= c.title %><br>
  <%= c.content %><br>
  <% if @post.image == nil? %>
  <%= c.image %><br>
  <% end %>
<% end %>
